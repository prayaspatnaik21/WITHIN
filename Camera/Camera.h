////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

#include <opencv2/opencv.hpp>
#include "ThreadSafeQueue.h"
#include "ImageProcessor.h"
#include <string_view>
#include <atomic>

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

constexpr std::string_view PIPELINE = "v4l2src device=/dev/video0 ! "
                                        "image/jpeg, width=640, height=480, framerate=30/1 ! "
                                        "jpegdec ! videoconvert ! "
                                        "appsink drop=true sync=false";

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class Camera
{   
    public:
        Camera(std::unique_ptr<ThreadSafeQueue<cv::Mat>> buffer);
        ~Camera();
        cv::Mat getFrame();
    private:
        std::unique_ptr<ThreadSafeQueue<cv::Mat>> buffer;
        cv::VideoCapture cap;
        std::thread imageCaptureThread;
        std::atomic<bool> running;
        std::shared_ptr<ImageProcessor> imageProcessorPtr;
        std::mutex bufferMtx;
        bool initCamera();
        void captureFrames();
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
   